# Field issue trackers carry officers' personal mobile numbers in bulk.
# They are worked here and encoded into Admin.gs registers, but the raw
# spreadsheets stay off the public repository. The same for screenshots
# of the Collector's browser and source documents worked into registers.
Issue/
Error/
Info/
# Source advisories the district hands over for publishing. The document
# belongs in Drive, published through the console — not carried in the repo.
Adv/
# The district's own rosters for the second register: 134 Gram Palana
# Officers and Revenue Inspectors with their personal mobile numbers, the GP
# office coordinates, and everything tests/gp-seed.js derives from them. THIS
# REPOSITORY IS PUBLIC. The workbook arrived on 18.09.2026 and was untracked
# only by the accident of landing after the day's last commit; a `git add -A`
# an hour earlier would have published it. Rosters have never belonged here.
Domain/
node_modules/

# Generated by tests/fixture-dashboard.js for the manual render pass.
# Deterministic and rebuilt on demand, so it is not carried in the repo.
tests/fixture-dashboard.json
tests/fixture-docs.json
tests/fixture-schedule.json
