/* ============================================================
   SJGP · district configuration
   ------------------------------------------------------------
   COPY THIS FILE TO  config.js  IN THE REPOSITORY, ONCE.
   Upgrade packs never contain config.js, so nothing here is
   ever overwritten and the address never has to be pasted back
   by hand after a publish.

   This is not a secret. It ships to every phone inside app.js
   and any browser can read it; it is kept apart only so that
   upgrades cannot blank it.
   ============================================================ */
window.SJGP_SERVER = 'https://script.google.com/macros/s/PASTE-YOUR-EXEC-ID-HERE/exec';
